{
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://api.smartlogix.example.com/api/:1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}